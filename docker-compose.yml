version: "3"

services:

  could-weather-postgres:
    image: debezium/postgres:13-alpine
    environment:
      - POSTGRES_DATABASE=could_weather_dev
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=aPPle45!
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/data/db

volumes:
  postgres-data:
